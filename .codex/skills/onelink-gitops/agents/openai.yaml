interface:
  display_name: "One Link GitOps"
  short_description: "Git, GitHub, and repo flow for One Link"
  default_prompt: "Use $onelink-gitops to handle GitHub workflow, submodule updates, skill sync, or upstream sync in One Link."
