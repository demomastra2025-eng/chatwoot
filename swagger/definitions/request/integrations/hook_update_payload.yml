type: object
properties:
  status:
    type: string
    description: The status of the integration
    example: disabled
  access_token:
    type: string
    description: Optional replacement secret token. Omit or send an empty value to keep the existing token.
    example: secret-token
  settings:
    type: object
    description: The settings required by the integration
    example: {}
