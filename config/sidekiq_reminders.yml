---
:verbose: false
:concurrency: <%= ENV.fetch("REMINDERS_SIDEKIQ_CONCURRENCY", 2) %>
:timeout: 25
:max_retries: 3

:queues:
  - reminders

production:
  :concurrency: <%= ENV.fetch("REMINDERS_SIDEKIQ_CONCURRENCY", 2) %>
staging:
  :concurrency: <%= ENV.fetch("REMINDERS_SIDEKIQ_CONCURRENCY", 2) %>
