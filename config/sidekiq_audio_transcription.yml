---
:verbose: false
:concurrency: <%= ENV.fetch("AUDIO_TRANSCRIPTION_SIDEKIQ_CONCURRENCY", 2) %>
:timeout: 25
:max_retries: 3

:queues:
  - audio_transcription

production:
  :concurrency: <%= ENV.fetch("AUDIO_TRANSCRIPTION_SIDEKIQ_CONCURRENCY", 2) %>
staging:
  :concurrency: <%= ENV.fetch("AUDIO_TRANSCRIPTION_SIDEKIQ_CONCURRENCY", 2) %>
